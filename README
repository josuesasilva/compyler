O projeto foi desenvolvido utilizando a linguagem Python versão 3.6.5.
Lebrando que não é possível executar o projeto com versões inferiores a 3.x.x.

Para executar basta acessar o terminal, em seguida o local do projeto.
Então executar:

$ cd caminho/do/projeto
$ python3 main.py tests/program.po

ou 

$ make run

Para execução de testes unitários:

$ make test


*Para executar corretamente é necessário estar na pasta raiz do projeto.

